import java.util.Scanner;

public class Revision {
    public static void main(String[] args) {
        String chaine = "gaye";
        int nombre = 4;

        // System.out.println(chaine.getClass()); // check variable type for String
        // System.out.println(((Object) nombre).getClass()); // check variable type for
        // int
        // int var = Integer.parseInt(chaine);
        // System.out.println(((Object) var).getClass());

        // for (int i = 0; i < chaine.length(); i++) {

        // }
        System.out.println(chaine.toUpperCase());
    }
}
